int main(int argc, char* argv[])
{
  int b = 1 < 2 ? 0:1;
  int c = !1;
  int d = 0 ^ 1;
  int e = ~1;
  bool g = 1==2 || 1 != 2;
  bool c = 1 <= 2 || 1 >= 3;
  bool d = f < g || g > e;
  int f = a + b * c / d % e;
  int g= (char) c;
  d -> e;
  f . g;
  d ->* e;
  f .*g;
  return 0;
}
