language: c++

compiler: clang

before_install:
  - pip install --user cpp-coveralls

install:
  
script: 
  - mkdir target
  - make test

after_success:
  - coveralls --exclude mylib --exclude test --gcov-options '\-lp'
